pipeline {
    agent any

    stages {
        stage('Actuate Deployment') {
            steps {
                sh '''
                   for resource in `ls resources`;do 
                      NAME=$(basename -s .yaml $resource)
                      gcloud compute instances update-from-file $NAME --source resources/$NAME.yaml
                   done
                '''
            }
        }
    }
}
